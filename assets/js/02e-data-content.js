"use strict";

/* ============================================================
   HAND-WRITTEN CONTENT
   Not regenerated by scripts/generate-data.rb. Edit freely.
   The canonical YAML descriptions are too thin to surface as-is
   (49/51 requirement descriptions are <300 chars; all 6 principle
   descriptions are <100). These rewrites give the microsite enough
   prose to actually orient a reader.
   ============================================================ */

/* Short labels for relationship pills — replaces underscored slugs in UI */
const RELATIONSHIP_LABELS = {
  addresses: "addresses",
  partially_addresses: "partially addresses",
  mitigates: "mitigates",
  tests_for: "tests for",
};
